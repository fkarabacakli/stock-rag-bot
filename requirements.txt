# Web Framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Telegram Bot
python-telegram-bot==21.7

# Vector Database — Docker Chroma (chromadb/chroma:latest) ile uyumlu olması için 1.x gerekir (REST API v2).
# 0.5.x istemcisi eski v1 endpoint'lerini kullanır; sunucu v2 döner → bağlantı / health hataları.
chromadb>=1.0.0,<2

# Embeddings (host'ta çalışır — Docker'a gitmez)
sentence-transformers==3.3.1

# LangChain — sadece Ollama inference için
langchain>=0.3.0
langchain-ollama>=0.2.0
langchain-core>=0.3.0

# Scheduler
apscheduler==3.10.4

# Web Scraping
beautifulsoup4==4.12.3
httpx>=0.27.0,<0.29.0
lxml==5.3.0
pymupdf>=1.23.0

# Data Processing
pandas==2.2.3

# Configuration
pydantic-settings==2.6.1
python-dotenv==1.0.1

# Utilities
tenacity==9.0.0
loguru==0.7.3
numpy>=1.26.0

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
