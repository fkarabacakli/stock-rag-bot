# Web Framework
fastapi>=0.115.5,<1
uvicorn[standard]>=0.32.1,<1

# Telegram Bot
python-telegram-bot>=21.7,<22

# Vector Database — requires 1.x for compatibility with Docker Chroma (chromadb/chroma:latest, REST API v2).
# The 0.5.x client uses old v1 endpoints; server responds with v2 -> connection/health failures.
chromadb>=1.0.0,<2

# Embeddings (runs on host — does not run in Docker)
sentence-transformers>=3.3.1,<4

# LangChain — used only for Ollama inference
langchain>=0.3.0
langchain-ollama>=0.2.0
langchain-core>=0.3.0

# Scheduler
apscheduler>=3.10.4,<4

# Web Scraping
beautifulsoup4>=4.12.3,<5
httpx>=0.27.0,<0.29.0
lxml>=5.3.0,<6
pymupdf>=1.23.0,<2

# Data Processing
pandas>=2.2.3,<3

# Configuration
pydantic-settings>=2.6.1,<3
python-dotenv>=1.0.1,<2

# Utilities
tenacity>=9.0.0,<10
loguru>=0.7.3,<1
numpy>=1.26.0,<3

# Testing
pytest>=8.3.4,<9
pytest-asyncio>=0.24.0,<1
